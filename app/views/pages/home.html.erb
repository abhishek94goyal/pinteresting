<div class="jumbotron center">
	<h1>Printeresting, huh?</h1>
	<% if user_signed_in?%>
		<p>Welcome, you are now logged in!</p>
	<% else %>
	<p><%= link_to "Login", new_user_session_path, class: "btn btn-lg btn-default"%> <%= link_to "Sign up", new_user_registration_path, class: "btn btn-lg btn-primary"%>
	<% end %>
	</p>
</div>